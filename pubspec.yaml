name: Akarat
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.8+21

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:   # ← Indented with 2 spaces under dependencies
    sdk: flutter





  readmore: ^3.0.0
  country_code_picker: ^3.4.1
  intl_country_data: ^1.2.9
  carousel_slider: ^5.1.1
  standard_searchbar: ^2.1.1
  sliding_switch: ^1.1.0
  syncfusion_flutter_sliders: ^31.2.18
  syncfusion_flutter_charts: ^31.2.18
  syncfusion_flutter_core: ^31.2.18
  tuna_flutter_range_slider: ^1.0.0
  smooth_page_indicator: ^2.0.1
  shared_preferences: ^2.5.4
  flutter_map: ^8.2.2
  cached_network_image: ^3.4.1
  http: ^1.6.0
  shimmer: ^3.0.0
  html_unescape: ^2.0.0
  flutter_html: ^3.0.0
  url_launcher: ^6.3.2
  flutter_phone_direct_caller: ^2.2.1
  dio: ^5.9.0
  crypto: ^3.0.7
  flutter_dotenv: ^6.0.0
  intl: ^0.20.2
  google_maps_flutter: ^2.14.0
  cupertino_icons: ^1.0.8
  flutter_secure_storage: ^10.0.0
  image_picker: ^1.2.1
  path_provider: ^2.1.5
  firebase_core: ^4.2.1
  firebase_auth: ^6.1.2
  google_sign_in: ^7.2.0
  gap: ^3.0.1
  easy_debounce: ^2.0.3
  device_info_plus: ^12.3.0
  dartz: ^0.10.1
  equatable: ^2.0.7
  get_it: ^9.2.0  # Already there, good for DI
  flutter_bloc: ^8.1.6  # NEW: For Bloc in Flutter
  bloc: ^8.1.4  # NEW: Core Bloc
  json_annotation: ^4.9.0  # NEW: If needed for JSON models (add build_runner in dev for code gen)
  provider: ^6.1.5+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  rename: ^3.0.2
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.3
  bloc_test: ^9.1.7  # NEW: For testing Blocs
  build_runner: ^2.4.12  # NEW: If adding json_annotation for code gen (run flutter pub run build_runner build)
  mocktail: ^1.0.3
# Rest remains the same (flutter_launcher_icons, flutter: uses-material-design: true, assets: ...)

flutter_launcher_icons:
  ios: true
  android: false
  image_path_ios: "assets/images/app-icon_new.png"
  remove_alpha_ios: true

# =============================================
# THIS IS THE ONLY PART THAT WAS MISSING
flutter:
  uses-material-design: true

  generate: true  # For automatic localization code generation

  assets:
    - .env                    # Your environment file
    - assets/images/          # This includes EVERY file inside assets/images/ and subfolders

